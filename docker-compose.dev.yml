# docker-compose for local dev
version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: perfilpro
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
  redis:
    image: redis:7
    ports:
      - '6379:6379'
